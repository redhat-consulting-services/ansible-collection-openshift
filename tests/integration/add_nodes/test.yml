# Copyright redhat-consulting-services Authors
# SPDX-License-Identifier: Apache-2.0
---
- name: Test add_nodes role
  hosts: localhost
  gather_facts: false
  vars:
    pull_secret: '{"auths":{"fake.registry":{"auth":"dGVzdDp0ZXN0"}}}'
    kubeconfig_path: "{{ test_artifact_dir }}/kubeconfig"
    case_file:
      path: "{{ case_file_path }}"
  tasks:
    - name: Create dummy kubeconfig for role env
      ansible.builtin.copy:
        dest: "{{ kubeconfig_path }}"
        mode: "0600"
        content: |
          apiVersion: v1
          kind: Config
          clusters: []
          contexts: []
          users: []

    - name: Run case
      ansible.builtin.include_tasks: run_case.yml
